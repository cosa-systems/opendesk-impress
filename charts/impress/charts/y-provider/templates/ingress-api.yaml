{{/*
SPDX-FileCopyrightText: 2025 Zentrum für Digitale Souveränität der Öffentlichen Verwaltung (ZenDiS) GmbH
SPDX-License-Identifier: Apache-2.0
*/}}
{{- if .Values.ingressCollaborationApi.enabled -}}
---
apiVersion: "{{ include "common.capabilities.ingress.apiVersion" . }}"
kind: "Ingress"
metadata:
  name: "{{ include "common.names.fullname" . }}-api"
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.additionalLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.additionalLabels "context" . ) | nindent 4 }}
    {{- end }}
  annotations:
    {{- if .Values.additionalAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.additionalAnnotations "context" . ) | nindent 4 }}
    {{- end }}
    {{- if .Values.ingressCollaborationApi.annotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.ingressCollaborationApi.annotations "context" . ) | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingressCollaborationApi.ingressClassName }}
  ingressClassName: {{ .Values.ingressCollaborationApi.ingressClassName | quote }}
  {{- end }}
  {{- if .Values.ingressCollaborationApi.tls.enabled }}
  tls:
    - hosts:
        - {{ coalesce .Values.ingressCollaborationApi.host .Values.global.fqdn | quote }}
      secretName: "{{ coalesce .Values.ingressCollaborationApi.tls.secretName .Values.global.tlsSecretName | default (printf "%s-tls" (include "common.names.fullname" . )) }}"
  {{- end }}
  rules:
    - host: {{ coalesce .Values.ingressCollaborationApi.host .Values.global.fqdn | quote }}
      http:
        paths:
          - path: {{ .Values.ingressCollaborationApi.path }}
            pathType: {{ .Values.ingressCollaborationApi.pathType }}
            backend: {{- include "common.ingress.backend" (dict "serviceName" (include "common.names.fullname" .) "servicePort" "http" "context" .) | nindent 14 }}
...
{{- end }}
