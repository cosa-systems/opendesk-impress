# SPDX-FileCopyrightText: 2025 Zentrum für Digitale Souveränität der Öffentlichen Verwaltung (ZenDiS) GmbH
# SPDX-License-Identifier: Apache-2.0
---
# The global properties are used to configure multiple charts at once.
global:
  collaborationServerSecret:
    # -- Value of collaboration server secret
    value: ""
    existingSecret:
      # -- Name of existing secret containing collaboration server secret, overrides provided value
      name: ""
      # -- Key where collaboration server secret is stored
      key: "collaborationServerSecret"

  # -- fully qualified domain name of this impress instance
  fqdn: ""

  # -- Define an ImagePullPolicy.
  #
  # Ref.: https://kubernetes.io/docs/concepts/containers/images/#image-pull-policy
  #
  # "IfNotPresent" => The image is pulled only if it is not already present locally.
  # "Always" => Every time the kubelet launches a container, the kubelet queries the container image registry to
  #             resolve the name to an image digest. If the kubelet has a container image with that exact digest cached
  #             locally, the kubelet uses its cached image; otherwise, the kubelet pulls the image with the resolved
  #             digest, and uses that image to launch the container.
  # "Never" => The kubelet does not try fetching the image. If the image is somehow already present locally, the
  #            kubelet attempts to start the container; otherwise, startup fails.
  #
  imagePullPolicy: "IfNotPresent"

  # -- Credentials to fetch images from private registry.
  # Ref: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
  #
  # imagePullSecrets:
  #   - "docker-registry"
  #
  imagePullSecrets: []

  # -- Container registry address.
  imageRegistry: "docker.io"

  # -- S3-compatible object store configuration shared by backend and frontend.
  # Setting values here overrides the individual backend/frontend sub-chart values.
  s3:
    # -- Hostname of the S3-compatible endpoint (e.g. s3.eu-central-2.wasabisys.com).
    # Used by the frontend nginx proxy (Host header + proxy_pass) and to derive AWS_S3_ENDPOINT_URL for the backend.
    host: ""
    # -- Port of the S3-compatible endpoint.
    port: 443
    # -- Bucket name. Must be identical between backend (AWS_STORAGE_BUCKET_NAME) and the
    # frontend nginx media proxy — set it once here instead of in both sub-charts separately.
    bucketName: "notes"
    # -- S3 region name (AWS_S3_REGION_NAME).
    regionName: ""
    # -- S3 Access Key ID
    accessKeyId:
      # -- Plain-text value; ignored when existingSecret.name is set.
      value: ""
      existingSecret:
        # -- Name of an existing secret containing the Access Key ID; overrides value above.
        name: ""
        # -- Key inside the existing secret.
        key: "awsS3AccessKeyId"
    # -- S3 Secret Access Key
    secretAccessKey:
      # -- Plain-text value; ignored when existingSecret.name is set.
      value: ""
      existingSecret:
        # -- Name of an existing secret containing the Secret Access Key; overrides value above.
        name: ""
        # -- Key inside the existing secret.
        key: "awsS3SecretAccessKey"

  # -- TLS secret name
  tlsSecretName: ""

  yProviderApiKey:
    # -- Value of Y Provider API key
    value: ""
    existingSecret:
      # -- Name of existing secret containing Y Provider API key, overrides provided value
      name: ""
      # -- Key where Y Provider API key is stored
      key: "yProviderApiKey"

# backend: {}

# frontend: {}

# y-provider: {}
...
